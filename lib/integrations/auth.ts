/**
 * Auth integration skeleton (build spec §7).
 *
 * Placeholder for a future authentication provider (e.g. Auth0, Clerk,
 * NextAuth). No auth is required for the current public marketing site.
 */

/** Whether authentication is wired up. Flip to `true` once configured. */
export const AUTH_ENABLED = false;
