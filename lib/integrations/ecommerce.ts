/**
 * E-commerce integration skeleton (build spec §7).
 *
 * Placeholder for a future checkout / payments layer (e.g. Stripe, Shopify
 * Storefront). The current site is a portfolio + lead-gen tool with no store.
 */

/** Whether e-commerce is wired up. Flip to `true` once configured. */
export const ECOMMERCE_ENABLED = false;
