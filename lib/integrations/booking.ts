/**
 * Booking integration skeleton (build spec §7).
 *
 * Placeholder for a future scheduling system (e.g. Cal.com, Calendly embed).
 * The contact form currently handles all enquiries.
 */

/** Whether a booking system is wired up. Flip to `true` once configured. */
export const BOOKING_ENABLED = false;
