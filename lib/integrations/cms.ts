/**
 * CMS integration skeleton (build spec §7).
 *
 * Placeholder for a future headless CMS (e.g. Sanity, Contentful, Payload).
 * Currently content is read from local Markdown/JSON via `lib/content.ts`.
 *
 * @example
 * // export async function getCmsPosts() {
 * //   const res = await fetch(`${process.env.CMS_API_URL}/posts`);
 * //   return res.json();
 * // }
 */

/** Whether a CMS is wired up. Flip to `true` once configured. */
export const CMS_ENABLED = false;
